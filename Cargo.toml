[package]
name = "xfer-webrtc"
version = "0.0.5"
edition = "2024"
publish = false

[lib]
name = "xfer_webrtc"
path = "src/lib.rs"

[[bin]]
name = "xfer-webrtc"
path = "src/main.rs"

[dependencies]
# Core
tokio = { version = "1", features = ["full"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
hex = "0.4"
rand = "0.9"
futures = "0.3"
log = "0.4"
rustls = { version = "0.23", default-features = false, features = ["aws_lc_rs"] }

# File handling
xxhash-rust = { version = "0.8", features = ["xxh64"] }

# HTTP client for relay fetching
reqwest = { version = "0.13", features = ["json", "multipart", "stream"] }

# Signaling payload encryption (shared-secret AEAD over the Nostr online path)
chacha20poly1305 = "0.10"
hkdf = "0.12"
sha2 = "0.10"

# Nostr signaling
nostr-sdk = "0.44.1"

# WebRTC transport
webrtc = "0.17"
bytes = "1"
crc32fast = "1.4"

# CLI (interactive prompts + arg parsing)
rustyline = "18"
clap = { version = "4", features = ["derive"] }
env_logger = "0.11"

[dev-dependencies]
tempfile = "3"

[profile.release]
strip = true
lto = "thin"
codegen-units = 1
panic = "abort"
